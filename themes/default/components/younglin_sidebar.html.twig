{# Younglin custom sidebar - rendered only when get_current_theme() == 'younglin' #}
<aside class="sidebar younglin-sidebar">
    <header class="main-header">
        <nav class="navbar" role="navigation">
            <div id="header-logo">
                <a class="logo" title="{{ ea.dashboardTitle|striptags }}" href="{{ path(ea.dashboardRouteName) }}">
                    <span class="logo-custom">{{ ea.dashboardTitle|raw }}</span>
                    <span class="logo-compact"><i class="fas fa-home"></i></span>
                </a>
            </div>
        </nav>
    </header>

    <div class="younglin-sidebar-inner">
        {{ include('components/younglin_main_menu.html.twig') }}
    </div>
</aside>
